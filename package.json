{
  "name": "moondsp-browser-tools",
  "private": true,
  "scripts": {
    "typecheck:graph": "node web/live/node_modules/typescript/bin/tsc --noEmit --strict --exactOptionalPropertyTypes --target ES2022 --module ESNext --moduleResolution Bundler --lib ES2022,DOM playwright-tests/graph-engine.types.ts",
    "build:browser": "moon build browser --target wasm-gc --release",
    "test:browser": "moon build --target wasm-gc --release && ./playwright-sync-wasm.sh && playwright test",
    "test:browser:headed": "moon build --target wasm-gc --release && ./playwright-sync-wasm.sh && playwright test --headed",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "wrangler": "^4.90.0"
  }
}
